discord.py>=2.3.0
# bot.py imports httpx directly to normalise transport timeouts into StageTimeout,
# so it is a direct dependency, not an openai transitive one.
# openai 3.x switched its transport to httpx2, which would leave httpx.Timeout
# incompatible with the value handed to AsyncOpenAI(timeout=...).
openai>=1.0.0,<3
httpx>=0.27,<1
duckduckgo-search>=6.0.0
